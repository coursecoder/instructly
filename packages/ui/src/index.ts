// Export UI components (placeholder for now)
export { default as Button } from './components/Button';

// Re-export shared types that UI components need
export type { User, Project } from '@instructly/shared';